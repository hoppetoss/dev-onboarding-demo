global:
  scrape_interval: 5s   # Pull metrics from our API every 5 seconds

scrape_configs:
  - job_name: "api"
    static_configs:
      - targets: ["api:8000"]   # "api" matches the service name in docker-compose
